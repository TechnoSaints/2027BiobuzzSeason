# Claude project config

This repository now keeps Claude-specific project files in a repo-local `.claude` directory so commands and skills travel with the project instead of living only in a global install.

## Structure

- `.claude/commands/` — project commands for Claude Code
- `.claude/skills/` — reusable skills for this repository

Add any Claude files here to keep them versioned with the project.
