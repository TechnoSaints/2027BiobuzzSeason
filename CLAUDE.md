# Project Claude configuration

Keep project-specific Claude commands and reusable skills in the repo-local `.claude/` folder so they stay with this repository.

Use:
- `.claude/commands/` for Claude command files
- `.claude/skills/` for reusable skills
